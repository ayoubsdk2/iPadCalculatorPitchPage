// Single-line title — bold, 25% larger, only "AI VOICE AGENT" in purple.
export function TitleBanner() {
  return (
    <h1 className="text-center font-black text-foreground whitespace-nowrap text-[clamp(1.3rem,3.05vw,2.6rem)] leading-none">
      LET'S CALCULATE REAL-WORLD{" "}
      <span className="text-primary">AI VOICE AGENT</span> IMPACT
    </h1>
  );
}
